---
import PageLayout from '~/layouts/PageLayout.astro';
---

<PageLayout title="Daniel Eze">
	<main id="main">
		<section class="hero v-spacing">
			<div class="container grid">
				<div class="hero__content">
					<h1 class="hero__title type-subhead">Hi, I’m Daniel.</h1>
				</div>
			</div>
		</section>
	</main>
</PageLayout>

<style lang="scss">
	@use 'lib/media-queries' as *;

	.hero__title {
		color: var(--color-text);
		font-weight: 600;
		margin-block-end: 0.5em;
		font-size: 44px;
		line-height: 1.2;
	}

	.hero__content {
		grid-column: 1/8;

		@media #{query('l')} {
			grid-column: 1/5;
		}
	}
</style>
